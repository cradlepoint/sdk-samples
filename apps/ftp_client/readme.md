# ftp_client
Demonstrates sending a file to an FTP server using `ftplib.py`. Server `speedtest.tele2.net` is used as an example.

## Expected Output

A file is sent to FTP server `speedtest.tele2.net`.
