# Files excluded from the deployed SDK package (development-only content).
# make.py reads this file (named 'buildignore') to prune the package.
# Developer scratch directory for throwaway validation harnesses.
tmp/
# Editor/backup scratch files must never ship in the package.
*.bak
